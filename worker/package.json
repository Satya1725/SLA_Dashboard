{
  "name": "earthre-sla-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute earthre-sla-db --file=../db/schema.sql",
    "db:init:remote": "wrangler d1 execute earthre-sla-db --remote --file=../db/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.134.0"
  }
}
